import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Cores principais
  static const Color primary = Color(0xFF033B63);
  static const Color primaryLight = Color(0xFF466A99);
  static const Color secondary = Color(0xFF9ED1FF);

  // Backgrounds
  static const Color background = Color(0xFFFFFFFF);
  static const Color card = Color(0xFFF5F5F5);

  // Textos
  static const Color textPrimary = Color(0xFF033B63);
  static const Color textSecondary = Color(0xFF466A99);

  // Estados
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFFC107);
  static const Color error = Color(0xFFE53935);

  // Outros
  static const Color divider = Color(0xFFE0E0E0);
}